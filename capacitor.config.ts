import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'slide menu app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
